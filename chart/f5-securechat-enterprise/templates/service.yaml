apiVersion: v1
kind: Service
metadata:
  name: {{ include "f5-securechat-enterprise.fullname" . }}
  labels:
    {{- include "f5-securechat-enterprise.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "f5-securechat-enterprise.selectorLabels" . | nindent 4 }}
